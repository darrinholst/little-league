<%= form_for([:admin, @division]) do |f| %>
  <%= errors_for(@division) %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %> or <%= link_to "cancel", admin_divisions_path %>
  </div>
<% end %>
